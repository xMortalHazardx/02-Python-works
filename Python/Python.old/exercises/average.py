#def lista(caiau):
    
    #for item in caiau:
        #soma = soma + item 
        #caiau = float(soma / len(caiau))
        #return caiau

   
def media(a):  
    
    soma = 0
    for item in a:
        soma = float(soma + item )
        media = len(a)
        media1 = soma / media
        return media1


print(media([1,2,6]))



